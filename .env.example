# --- Database (shared by web + etl) ---
DATABASE_URL="postgresql://rankpath:rankpath@localhost:5433/rankpath?schema=public"

# --- Next.js web ---
NEXT_PUBLIC_SITE_NAME="RankPath"
NEXT_PUBLIC_DEFAULT_LOCALE="en"

# Admin upload auth (single-tenant; replace with real auth for production)
ADMIN_API_TOKEN="change-me-locally"

# --- ETL ---
RAW_DIR="./data/raw"
NORMALIZED_DIR="./data/normalized"
